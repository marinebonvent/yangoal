<div class="min-h-screen flex flex-col md:flex-row">
    <!-- Desktop Sidebar -->
    <div class="hidden md:block bg-white w-64 shadow-lg">
        <div class="p-6 border-b border-gray-200">
            <div class="flex items-center justify-start">
                <span class="ml-3 text-xl font-semibold">Hello {{user.Lastname}}</span>
            </div>
        </div>
        <nav class="mt-2">
            <a href="{{ path('app_message') }}" class="flex items-center py-3 px-6 text-gray-700 hover:bg-gray-100 hover:text-blue-500 group transition-colors" id="profile-tab">
                <i class="fas fa-message mr-3 text-gray-400 group-hover:text-blue-500"></i>
                <span>Message</span>
            </a>
            <a href="{{ path('app_profil') }}" class="flex items-center py-3 px-6 text-gray-700 hover:bg-gray-100 hover:text-blue-500 group transition-colors" id="profile-tab">
                <i class="fas fa-user mr-3 text-gray-400 group-hover:text-blue-500"></i>
                <span>Mes Données</span>
            </a>
            <a href="{{ path('app_bons') }}" class="flex items-center py-3 px-6 text-gray-700 hover:bg-gray-100 hover:text-blue-500 group transition-colors" id="coupons-tab">
                <i class="fas fa-ticket-alt mr-3 text-gray-400 group-hover:text-blue-500"></i>
                <span>Bons de Réduction</span>
            </a>
            <a href="{{ path('app_historique') }}" class="flex items-center py-3 px-6 text-gray-700 hover:bg-gray-100 hover:text-blue-500 group transition-colors" id="orders-tab">
                <i class="fas fa-shopping-bag mr-3 text-gray-400 group-hover:text-blue-500"></i>
                <span>Historique Commandes</span>
            </a>
            <a href="{{ path('app_logout') }}" class="flex items-center py-3 px-6 text-gray-700 hover:bg-gray-100 hover:text-red-500 group transition-colors mt-6">
                <i class="fas fa-sign-out-alt mr-3 text-gray-400 group-hover:text-red-500"></i>
                <span>Déconnexion</span>
            </a>
        </nav>
    </div>

    <!-- Mobile Header -->
    <div class="md:hidden w-full bg-white shadow-lg">
        <div class="p-4 border-b border-gray-200 flex justify-center items-center">
            <span class="text-lg font-semibold">Hello {{user.Lastname}}</span>
        </div>
    </div>
    
    <!-- Mobile Tab Bar (always visible) -->
    <div class="md:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 flex justify-around px-1 py-2 z-50">
        <a href="{{ path('app_message') }}" class="flex flex-col items-center text-gray-600 hover:text-blue-500 {% if app.request.get('_route') == 'app_message' %}text-blue-500{% endif %}">
            <i class="fas fa-message text-lg"></i>
            <span class="text-xs mt-1">Message</span>
        </a>
        <a href="{{ path('app_profil') }}" class="flex flex-col items-center text-gray-600 hover:text-blue-500 {% if app.request.get('_route') == 'app_profil' %}text-blue-500{% endif %}">
            <i class="fas fa-user text-lg"></i>
            <span class="text-xs mt-1">Profil</span>
        </a>
        <a href="{{ path('app_bons') }}" class="flex flex-col items-center text-gray-600 hover:text-blue-500 {% if app.request.get('_route') == 'app_bons' %}text-blue-500{% endif %}">
            <i class="fas fa-ticket-alt text-lg"></i>
            <span class="text-xs mt-1">Bons</span>
        </a>
        <a href="{{ path('app_historique') }}" class="flex flex-col items-center text-gray-600 hover:text-blue-500 {% if app.request.get('_route') == 'app_historique' %}text-blue-500{% endif %}">
            <i class="fas fa-shopping-bag text-lg"></i>
            <span class="text-xs mt-1">Commandes</span>
        </a>
        <a href="{{ path('app_logout') }}" class="flex flex-col items-center text-gray-600 hover:text-red-500">
            <i class="fas fa-sign-out-alt text-lg"></i>
            <span class="text-xs mt-1">Quitter</span>
        </a>
    </div>

    <!-- Content container - add padding-bottom on mobile to account for fixed navbar -->
 
