{% extends 'base.html.twig' %}

{% block title %}Erreur de Paiement{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full">
        {# Card Container #}
        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            {# Header Section #}
            <div class="bg-red-50 px-6 py-4 border-b border-red-100">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <svg class="h-8 w-8 text-red-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-lg font-semibold text-red-800">Paiement Non Effectué</h2>
                        <p class="text-sm text-red-600">Une erreur est survenue lors du traitement</p>
                    </div>
                </div>
            </div>

            {# Main Content #}
            <div class="px-6 py-6">
                <div class="space-y-4">
                    {# Reassurance Message #}
                    <div class="bg-green-50 rounded-lg px-4 py-3 border border-green-100">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <svg class="h-5 w-5 text-green-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                            </div>
                            <div class="ml-3">
                                <p class="text-sm text-green-700">Aucun montant n'a été débité de votre compte</p>
                            </div>
                        </div>
                    </div>

                    {# Detailed Information #}
                    <div class="space-y-3 text-gray-600">
                        <p class="text-sm">
                            Nous n'avons pas pu traiter votre paiement en raison d'un problème technique. Voici ce que vous pouvez faire :
                        </p>
                        <ul class="list-disc list-inside space-y-2 text-sm pl-2">
                            <li>Vérifier les informations de votre carte</li>
                            <li>Utiliser un autre mode de paiement (les détails ont été envoyés par email)</li>
                            <li>Vérifier votre dossier de courriers indésirables si vous ne trouvez pas l'email</li>
                        </ul>
                    </div>
                </div>
            </div>

            {# Actions Section #}
            <div class="bg-gray-50 px-6 py-4 border-t border-gray-100">
                <div class="flex flex-col sm:flex-row sm:space-x-4 space-y-3 sm:space-y-0">
                    <a href="{{ path('app_cart') }}" class="inline-flex justify-center items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                        Réessayer le paiement
                    </a>
                    <a href="{{ path('app_contacte') }}" class="inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                        Contacter le support
                    </a>
                </div>
            </div>
        </div>

        {# Help Section #}
        <div class="mt-6 text-center">
            <p class="text-sm text-gray-500">
                Besoin d'aide ? 
                <a href="{{ path('app_cgv') }}" class="font-medium text-red-600 hover:text-red-500">
                    Consultez notre FAQ
                </a>
              
            </p>
        </div>
    </div>
</div>
{% endblock %}
